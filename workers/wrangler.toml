name = "shanghai-tours-form"
main = "src/index.js"
compatibility_date = "2025-04-01"

# 如果要用自定义域名（推荐），取消注释并替换为你的域名
# routes = [
#   { pattern = "form.shanghaitours.com/*", zone_id = "YOUR_ZONE_ID" }
# ]

# 环境变量：飞书 Webhook 地址（也可以通过 Cloudflare Dashboard 设置）
# [vars]
# FEISHU_WEBHOOK = "https://open.feishu.cn/open-apis/bot/v2/hook/xxx"